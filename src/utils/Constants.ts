// The ratio of one CSS-pixel to one physical pixel. Value of 1 means DPI = DEFAULT_DPI.
export const DEVICE_PIXEL_RATIO: number = window.devicePixelRatio;
// Default DPI for screens.
export const DEFAULT_DPI: number = 96;
export const MM_PER_INCH: number = 25.4;
export const PATCH_OUTLINE_THICKNESS: number = 1;
export const PATCH_OUTLINE_COLOR: number = 0xFFFFFF;